name: credit_risk
version: '1.0.0'
config-version: 2
profile: credit_risk
model-paths: ['models']
test-paths: ['tests']
target-path: target
models:
  credit_risk:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
